# BOOTSTRAP file generated by mzvk :: manually :: 14:33 28.06.2018
system {
    host-name sw-init-phase1;
    protect: root-authentication { 
        encrypted-password "$1$qDqe$WXTlKTB3SSt6IW.6MWIKH."; ## SECRET-DATA
    }  
    login { message "\n ------------\n THIS DEVICE WAS CONFIGURED USING: JMT [Phase1] \n ----------\n"; }
    services { ssh; }
    syslog { file messages { any notice; authorization info; } }
    delete: commit; 
}
chassis { delete: auto-image-upgrade; }
interfaces { 
    interface-range ZTP-ALL {
        member-range ge-0/0/0 to ge-0/0/11;
        unit 0 { family ethernet-switching { port-mode trunk; vlan { members ADMIN; } } }
    }
    vlan { 
        unit 2 { family inet { dhcp { vendor-id ZTP-TEST-PHASE1; } } } 
    }
}
event-options {
    generate-event { ZTP-BEACON time-interval 120; }
    policy ZTP-CALL-HOME {
        events ZTP-BEACON;
        then {
            execute-commands {
                commands {              
                    "op url ftp://157.163.140.145/ztp-phase2.slax";
                }
            }
        }
    }
}
vlans { ADMIN { vlan-id 2; l3-interface vlan.2; } }
